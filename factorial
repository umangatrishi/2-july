#include <iostream>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int num;
        cin >> num;
        int factorial = 1;
        for (int i = 2; i <= num; ++i) {
            factorial *= i;
        }
        cout << factorial << endl;
    }
    return 0;
}
